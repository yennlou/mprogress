import React from 'react'
import {
  ProgressWrapper,
  ProgressInner
} from './ProgressBar.styles'

const ProgressBar = () => {
  return (
    <ProgressWrapper>
      <ProgressInner />
    </ProgressWrapper>
  )
}

export default ProgressBar
